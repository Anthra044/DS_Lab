#include<stdio.h>
int f(int n)
{
    if(n==0 || n==1)
    {
        return (1);
    }
    return n * f(n-1);
}
void main() 
{
    int n, fact ;
    printf("Enter a number: ");
    scanf("%d",&n);
    fact = f(n);
    printf("Factorial of %d is %d", n, fact);
}
